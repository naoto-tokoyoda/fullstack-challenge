'use strict';

/**
 * page-without-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::page-without-image.page-without-image');
